services:
  app:
    build: .
    ports: ["8000:8000"]
    env_file: .env
    volumes: ["./data:/data"]
    command: uvicorn app.api.main:app --host 0.0.0.0 --port 8000
  # GPU worker profile (prod ASR): uncomment with nvidia runtime
  # worker-gpu:
  #   build: { dockerfile: Dockerfile.gpu }
  #   profiles: [gpu]
